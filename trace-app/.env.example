# API base URL for the React app (default: http://localhost:3001/api)
# VITE_API_URL=http://localhost:3001/api

# Server port (default: 3001)
# PORT=3001

# Electron: use npm run electron:dev (desktop) or electron:build (installer).
# Desktop build uses .env.electron (VITE_API_URL=/api).
